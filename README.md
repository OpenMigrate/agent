# agent
Agent component of the OpenMigrate platform — designed for cloud-agnostic OS-level server replication. Captures disk snapshots, tracks live changes, and transfers data to the replication backend or cloud storage. Supports both self-replicating and forwarding-only modes for flexible deployment.
